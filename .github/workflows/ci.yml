name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify application
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: No new Lovable runtime dependencies
        id: lovable_runtime
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          # Portability baseline: after this point AICIS implementation is direct-to-repo.
          # Legacy Lovable references are being removed incrementally, but no new runtime
          # dependency on Lovable Cloud, Lovable AI Gateway, or Lovable auth may be added.
          BASE_SHA="bb83a8ae06ce9df1336a5b158521e87d818de144"
          ADDED_LINES="$(git diff --unified=0 "$BASE_SHA" HEAD -- '*.ts' '*.tsx' '*.js' '*.mjs' '*.json' '*.toml' '*.yml' '*.yaml' \
            | grep '^+' \
            | grep -v '^+++' || true)"

          if printf '%s\n' "$ADDED_LINES" | grep -Eqi 'ai\.gateway\.lovable\.dev|LOVABLE_API_KEY|@lovable\.dev/cloud-auth|lovable cloud'; then
            printf '%s\n' "$ADDED_LINES" | grep -Ei 'ai\.gateway\.lovable\.dev|LOVABLE_API_KEY|@lovable\.dev/cloud-auth|lovable cloud' || true
            echo "New Lovable runtime dependency detected. AICIS must remain provider/host portable."
            exit 1
          fi

          echo "No new Lovable runtime dependencies detected."

      - name: Strict lint — post-Phase-4 code
        id: strict_lint
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          BASE_SHA="e2181170b8de9996a1da7fba797511579ee23a08"
          mapfile -t FILES < <(git diff --name-only --diff-filter=ACMR "$BASE_SHA" HEAD -- '*.ts' '*.tsx')
          if [ ${#FILES[@]} -eq 0 ]; then
            echo "No TypeScript files changed since Phase 4 baseline."
            exit 0
          fi
          printf 'Strict-linting %s file(s):\n' "${#FILES[@]}"
          printf ' - %s\n' "${FILES[@]}"
          bunx eslint "${FILES[@]}" --max-warnings 0

      - name: Legacy lint debt ratchet
        id: lint_ratchet
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          set +e
          bunx eslint . -f json > eslint-report.json
          ESLINT_STATUS=$?
          set -e
          if [ "$ESLINT_STATUS" -gt 1 ]; then
            echo "ESLint execution/configuration failed with status $ESLINT_STATUS"
            exit "$ESLINT_STATUS"
          fi
          node .github/scripts/check-lint-baseline.mjs eslint-report.json 1385 71

      - name: Typecheck
        id: typecheck
        if: always()
        continue-on-error: true
        run: bun run typecheck

      - name: Production build
        id: build
        if: always()
        continue-on-error: true
        run: bun run build

      - name: Verification summary
        if: always()
        shell: bash
        run: |
          echo "Lovable runtime gate: ${{ steps.lovable_runtime.outcome }}"
          echo "Strict lint: ${{ steps.strict_lint.outcome }}"
          echo "Lint debt ratchet: ${{ steps.lint_ratchet.outcome }}"
          echo "Typecheck: ${{ steps.typecheck.outcome }}"
          echo "Build: ${{ steps.build.outcome }}"

          FAILED=0
          [ "${{ steps.lovable_runtime.outcome }}" = "success" ] || FAILED=1
          [ "${{ steps.strict_lint.outcome }}" = "success" ] || FAILED=1
          [ "${{ steps.lint_ratchet.outcome }}" = "success" ] || FAILED=1
          [ "${{ steps.typecheck.outcome }}" = "success" ] || FAILED=1
          [ "${{ steps.build.outcome }}" = "success" ] || FAILED=1

          if [ "$FAILED" -ne 0 ]; then
            echo "One or more verification gates failed."
            exit 1
          fi

          echo "All verification gates passed."
